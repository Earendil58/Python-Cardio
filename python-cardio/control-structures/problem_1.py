# Diseña un programa que lea un número flotante por teclado y muestre por pantalla
# el mensaje « El número es negativo » solo si el número es menor que cero.

number = float(input('Enter a number: '))

